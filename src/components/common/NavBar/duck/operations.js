import actions from "./actions";

const login = actions.login;
const logoff = actions.logoff;

const on = actions.on;
const off = actions.off;


export default {
    login,
    logoff,
    on,
    off
}