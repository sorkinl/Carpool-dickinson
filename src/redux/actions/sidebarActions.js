import { 
    AT_HOME, 
    AT_CHAT, 
    AT_MY_TRIPS 
} from "../constants/sidebar-types";

export const locateUser = (payload) => {
    // return (dispatch) => {
    //     if(payload.location === "at-home") {
    //         console.log("in-progree reducer: ");
    //         dispatch({ type: AT_HOME, payload: payload.location });
    //     } else if (payload.location === "at-chat")  {
    //         dispatch({ type: AT_CHAT, payload: payload.location });
    //     }
    //     else if (payload.location === "at-my-trips")  {
    //         dispatch({ type: AT_MY_TRIPS, payload: payload.location });
    //     }
    // }
}